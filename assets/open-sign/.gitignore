# Exploration sketches are regenerated by build_open_sign.py but deliberately
# NOT shipped in the spec-origin repo (decided 2026-08-02). The full contact
# sheet lives in the ops/ historical copy. Without this rule a plain `git add -A`
# after any regenerate silently re-adds them, which is how they landed once.
assets/open-sign/out/direction-*.svg
